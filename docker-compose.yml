version: "3.1"

services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 6FuSQ5HxgHz1
      MYSQL_DATABASE: geek_shopping_product_api
    ports:
      - 3306:3306
